package com.panchayat.model;

/** Direct, non-complaint service bookings residents can raise. */
public enum ServiceType {
    PLUMBER,
    ELECTRICIAN,
    CLEANING,
    PEST_CONTROL,
    CARPENTER,
    PAINTER,
    GYM_TRAINER,
    OTHER
}
