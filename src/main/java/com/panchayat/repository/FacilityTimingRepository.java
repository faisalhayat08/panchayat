package com.panchayat.repository;

import com.panchayat.model.FacilityTiming;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FacilityTimingRepository extends JpaRepository<FacilityTiming, Long> {
}
